if skill "Magery" < 80
    overhead "Flamestrike only"
    if hp > 45 and mana >= 40
        hotkey "Flamestrike"
        wft
        hotkey 'Target Self'
        wait 500
    endif
    if mana <= 40 and not findbuff "actively meditating"
        hotkey 'Meditation'
        wait 3500
    endif
    wait 500
    loop
endif