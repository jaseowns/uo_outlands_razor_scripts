# This is an automated backup - check out https://outlands.uorazorscripts.com/script/ccc15010-78b8-4678-90f8-85904f578ef7 for latest
# Automation by Jaseowns
## Script: Lockbox Management Part Two
## Created by: jolanar#0
#############################################

if findtype 'copper key' as key and findtype 'small crate' as box
    getlabel box desc
    if "stones" in desc
        overhead "Locking"
        dclick key
        wft
        target box
    else
        overhead "Already Locked!"
    endif
endif

if findtype 'copper key' as key and findtype 'crate' as box
    getlabel box desc
    if "stones" in desc
        overhead "Locking"
        dclick key
        wft
        target box
    else
        overhead "Already Locked!"
    endif
endif