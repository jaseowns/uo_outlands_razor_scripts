if skill "Magery" < 80
    if skill "Magery" < 75
        overhead "Flamestrike to 75"
        if hp > 45 and mana >= 40
            hotkey "Flamestrike"
            wft
            hotkey 'Target Self'
            wait 500
        endif
    else
        overhead "Invisibility to 80"
        if mana >= 40
            hotkey "Invisibility"
            wft
            hotkey 'Target Self'
            wait 500
        endif
    endif
    
    if mana <= 40 and not findbuff "actively meditating"
        hotkey 'Meditation'
        wait 3500
    endif
    loop
endif