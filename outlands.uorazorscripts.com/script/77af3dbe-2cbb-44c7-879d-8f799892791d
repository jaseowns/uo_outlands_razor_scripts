# This is an automated backup - check out https://outlands.uorazorscripts.com/script/77af3dbe-2cbb-44c7-879d-8f799892791d for latest
# Automation by Jaseowns
## Script: Train Magic Resist
## Created by: psychotics88#0
#############################################

#Make sure to stand next to your shelf and update below your shelf serial number using >info

while skill 'MagicResist' < 80

  # Start summoning only if no followers alive
  if followers = 0
    clearall   # safe here, happens once before summoning

    # Summon elementals until 2 are created (4 slots used)
    while followers < 3
      cast "Fire Elemental"
      wait 1500   # wait for elemental to fully appear

      # Immediately after the second elemental is summoned
      if followers = 4
        #Change "SERIAL##" to your shelf serial number using >info
        menu SERIAL## 1 
        say "all kill"
        wft
        target self
      endif
    endwhile
  endif

  # Heal if HP lost
  if diffhits > 30
    cast "Greater Heal"
    wait 200
    target self
    wait 200

    # Only clearall if followers are gone, prevents spam
    if followers = 0
      clearall
    endif
  endif

endwhile
