if skill "Magery" < 80
    
    if skill "Magery" < 75
        overhead "Invisibility to 75"
        if mana >= 40
            hotkey "Invisibility"
            wft
            hotkey 'Target Self'
            wait 500
        endif
    else
        overhead "Flamestrike now"
        if hp < 50
            dclick 0x402A7363
            wft
            hotkey "Target Self"
            wait 12000
        elseif hp > 45 and mana >= 40
            hotkey "Flamestrike"
            wft
            hotkey 'Target Self'
            wait 500
        endif
    endif
    
    if mana =< 40 and not findbuff "actively meditating"
        hotkey 'Meditation'
        wait 3500
    endif
    loop
endif