if skill "Magery" < 80
    overhead "Flamestrike only"
    if hp < 50
        dclick 0x402A7363
        wft
        hotkey "Target Self"
        wait 12000
    elseif hp > 45 and mana >= 40
        hotkey "Flamestrike"
        wft
        hotkey 'Target Self'
        wait 500
    endif
    
    if mana =< 40 and not findbuff "actively meditating"
        hotkey 'Meditation'
        wait 3500
    endif
    loop
endif